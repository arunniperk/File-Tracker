---
phase: 04-data-safety-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/FileTracker.Core/Services/IBackupService.cs
  - src/FileTracker.App/Services/BackupService.cs
  - src/FileTracker.Core/Models/IntegrityCheckResult.cs
  - src/FileTracker.App/FileTracker.App.csproj
  - src/FileTracker.App/App.xaml.cs
  - src/FileTracker.App/ViewModels/MainViewModel.cs
  - src/FileTracker.App/MainWindow.xaml
  - tests/FileTracker.Tests/Services/BackupServiceTests.cs
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "User can create a backup of the database and all attachments to any chosen folder location"
    - "Backup produces a single timestamped .zip file containing the database file and full attachments directory"
  artifacts:
    - path: "src/FileTracker.Core/Services/IBackupService.cs"
      provides: "Backup service contract — CreateBackupAsync"
      exports: ["IBackupService"]
    - path: "src/FileTracker.App/Services/BackupService.cs"
      provides: "BackupService — SqliteConnection.BackupDatabase() + System.IO.Compression.ZipFile"
      min_lines: 80
    - path: "tests/FileTracker.Tests/Services/BackupServiceTests.cs"
      provides: "Backup integration tests"
      min_lines: 100
  key_links:
    - from: "BackupService.CreateBackupAsync"
      to: "SqliteConnection.BackupDatabase"
      via: "Microsoft.Data.Sqlite backup API"
      pattern: "\\.BackupDatabase\\("
    - from: "BackupService.CreateBackupAsync"
      to: "attachments directory"
      via: "ZipFile.CreateFromDirectory"
      pattern: "ZipFile\\.CreateFromDirectory"
    - from: "MainViewModel.BackupCommand"
      to: "IBackupService.CreateBackupAsync"
      via: "DI-injected service call"
      pattern: "_backupService\\.CreateBackupAsync"
---

<objective>
Deliver manual one-click backup (DATA-01). User clicks a Backup button, selects a destination folder, and receives a timestamped .zip file containing the SQLite database and all attachments.

Purpose: Give non-technical staff a single-click way to safeguard their data per DATA-01 and Pitfall 1 (existential risk of data loss).
Output: IBackupService contract, BackupService implementation, Backup menu item in MainWindow, and integration tests.
</objective>

<execution_context>
@C:/Users/Office/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Office/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-data-safety-management/04-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
<!-- Existing patterns executor needs for DI registration and service wiring -->

From src/FileTracker.App/App.xaml.cs (service registration pattern):
```csharp
// Services are registered as Singleton
builder.Services.AddSingleton<IAttachmentService, AttachmentService>();

// ViewModels are registered as Transient
builder.Services.AddTransient<MainViewModel>();

// SqliteConnection is a registered Singleton
builder.Services.AddSingleton<SqliteConnection>(_ => { ... });
```

From src/FileTracker.App/ViewModels/MainViewModel.cs (command + window-open pattern):
```csharp
// Commands use [RelayCommand] attribute
[RelayCommand]
private void OpenManagePositions() { ... }

// Resolving transient VMs at runtime:
var app = (App)System.Windows.Application.Current;
var vm = app.Services.GetRequiredService<ManagePositionsViewModel>();
```

From src/FileTracker.App/MainWindow.xaml (button integration pattern):
```xml
<Button Content="⚙ Positions" Command="{Binding OpenManagePositionsCommand}" />
```

From src/FileTracker.App/Services/AttachmentService.cs (service constructor pattern):
```csharp
// Services receive SqliteConnection + other services via DI
public AttachmentService(IDocumentRepository docRepo, IAttachmentRepository attachmentRepo,
    ILogger<AttachmentService> logger, string? attachmentRoot = null)

// Default paths use Environment.SpecialFolder.LocalApplicationData
_attachmentRoot = attachmentRoot ?? Path.Combine(
    Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData),
    "FileTracker", "attachments");
```

Database and attachments paths:
- Database: `%LocalAppData%\FileTracker\filetracker.db`
- Attachments: `%LocalAppData%\FileTracker\attachments\`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IBackupService contract, BackupService implementation, and integration tests</name>
  <files>
    src/FileTracker.Core/Models/IntegrityCheckResult.cs
    src/FileTracker.Core/Services/IBackupService.cs
    src/FileTracker.App/Services/BackupService.cs
    tests/FileTracker.Tests/Services/BackupServiceTests.cs
    src/FileTracker.App/FileTracker.App.csproj
    src/FileTracker.App/App.xaml.cs
  </files>
  <behavior>
    - Test 1: CreateBackupAsync creates a .zip file at destination containing filetracker_backup.db and attachments/ directory
    - Test 2: CreateBackupAsync backup .db file is a valid SQLite database (can open and query)
    - Test 3: CreateBackupAsync .zip filename matches pattern FileTracker_Backup_YYYY-MM-DD_HHmmss.zip
    - Test 4: CreateBackupAsync throws when destination folder does not exist
    - Test 5: CreateBackupAsync completes successfully when attachments directory is empty (no attachments yet)
  </behavior>
  <action>
    Implement DATA-01 manual backup per D-01 (ZIP with DB + attachments), D-02 (folder picker called by ViewModel), D-03 (FileTracker_Backup_YYYY-MM-DD_HHmmss.zip naming).

    **Step 0 — Add UseWindowsForms to csproj:**
    Edit `src/FileTracker.App/FileTracker.App.csproj` — add `<UseWindowsForms>true</UseWindowsForms>` inside the `<PropertyGroup>` block. This is a framework reference (not a NuGet package) needed for `System.Windows.Forms.FolderBrowserDialog` in the ViewModel.

    **Step 1 — Create IntegrityCheckResult model:**
    Create `src/FileTracker.Core/Models/IntegrityCheckResult.cs`:
    - Namespace: `FileTracker.Core.Models`
    - Properties: `bool IsOk`, `string Message`
    - This is needed by IBackupService.CheckDatabaseIntegrityAsync signature (Plan 02 uses it, define it now so IBackupService is stable)

    **Step 2 — Create IBackupService interface:**
    Create `src/FileTracker.Core/Services/IBackupService.cs`:
    ```csharp
    namespace FileTracker.Core.Services;
    public interface IBackupService
    {
        Task<string> CreateBackupAsync(string destinationFolder, CancellationToken ct = default);
    }
    ```

    **Step 3 — Create BackupService:**
    Create `src/FileTracker.App/Services/BackupService.cs`:
    - Namespace: `FileTracker.App.Services`
    - Constructor: `(SqliteConnection db, ILogger<BackupService> logger)` — inject the singleton SqliteConnection and ILogger
    - `CreateBackupAsync(string destinationFolder, CancellationToken ct)`:
      1. Compute backup filename: `FileTracker_Backup_{DateTime.Now:yyyy-MM-dd_HHmmss}.zip`
      2. Create a temp staging directory: `Path.Combine(Path.GetTempPath(), $"ft_backup_{Guid.NewGuid():N}")`
      3. Open a second SqliteConnection to the temp db path, call `db.BackupDatabase(tempConn)` — this uses SQLite's safe backup API
      4. Use `ZipFile.CreateFromDirectory(tempDir, zipPath)` — temp dir must contain the .db file and the attachments/ directory
      5. Copy the attachments directory into the temp dir (use recursive Directory.CreateDirectory + file copy, or just zip directly)
      6. Clean up temp staging directory in a try/finally or try/catch
      7. Log success via `_logger.LogInformation("Backup created: {Path}", zipPath)`
      8. Return the full path to the .zip file
    - Get attachment root path: `Path.Combine(Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData), "FileTracker", "attachments")`
    - Get database path: `_db.DataSource` from the SqliteConnection
    - ALL I/O operations inside try/catch with meaningful exceptions on failure
    - Use `ct.ThrowIfCancellationRequested()` before long operations

    **Step 4 — Create BackupServiceTests:**
    Create `tests/FileTracker.Tests/Services/BackupServiceTests.cs`:
    - Class implements `IAsyncLifetime`
    - In `InitializeAsync`: create in-memory SQLite DB, create Documents + Attachments tables (same schema as DatabaseInitializer), seed test data, create temp directories for attachments and destination
    - In `DisposeAsync`: clean up temp directories
    - Write the 5 behavior tests specified above (RED first, then GREEN with implementation)
    - Test the backup .zip by extracting it and verifying contents

    **Step 5 — Register in DI:**
    Edit `src/FileTracker.App/App.xaml.cs`:
    - Add `builder.Services.AddSingleton<IBackupService, BackupService>();` in the Services registration block (after IReportService line)
  </action>
  <verify>
    <automated>dotnet test tests/FileTracker.Tests/FileTracker.Tests.csproj --filter "BackupService"</automated>
  </verify>
  <done>
    All BackupService tests pass. `IBackupService.CreateBackupAsync` is registered in DI, creates valid .zip files with DB + attachments, follows D-01/D-03 naming conventions.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Backup UI — menu button in MainWindow, pick destination folder, show success feedback</name>
  <files>
    src/FileTracker.App/MainWindow.xaml
    src/FileTracker.App/ViewModels/MainViewModel.cs
  </files>
  <action>
    Wire the Backup command into the existing Documents tab toolbar (next to Reports and Positions buttons).

    **Step 1 — Add BackupCommand to MainViewModel:**
    Edit `src/FileTracker.App/ViewModels/MainViewModel.cs`:
    - Inject `IBackupService _backupService` via constructor (add to parameter list)
    - Add field: `private readonly IBackupService _backupService;`
    - Add `[RelayCommand]` method `private async Task BackupAsync()`:
      1. Use `System.Windows.Forms.FolderBrowserDialog` with `ShowNewFolderButton = true` and `Description = "Select folder to save backup"`
      2. If dialog result is OK and path is not empty, call `await _backupService.CreateBackupAsync(selectedPath)`
      3. Show `MessageBox.Show($"Backup created successfully:\n{backupPath}", "Backup Complete", MessageBoxButton.OK, MessageBoxImage.Information)`
      4. Wrap in try/catch — show `MessageBox.Show($"Backup failed: {ex.Message}", "Backup Error", MessageBoxButton.OK, MessageBoxImage.Error)` on failure
      5. Log via a logger if available, or just let BackupService log
    - The FolderBrowserDialog must be invoked on the UI thread — since RelayCommand already runs on UI thread, no Dispatcher needed.

    **Step 2 — Add Backup button to MainWindow:**
    Edit `src/FileTracker.App/MainWindow.xaml`:
    - In the Documents tab toolbar (Row 1 of the search GroupBox), add a Backup button between the Reports button (Column 6) and Positions button (Column 7)
    - Shift existing buttons: Reports → Column 5, Backup → Column 6, Positions → Column 7
    - Button XAML: `<Button Grid.Row="1" Grid.Column="6" Content="💾 Backup" Command="{Binding BackupCommand}" Width="75" Margin="8,4,4,0" />`
    - Update Grid.Column values for Reports (Column 5 → width 70), Backup (Column 6 → width 75), Positions (Column 7 → width 80)
    - If column indices in the Grid are tightly coupled, be precise about shifting — grep for current `Grid.Column=` assignments before editing.

    **IMPORTANT:** The MainWindow.xaml Grid has ColumnDefinitions up to 8 (0-8). Verify current layout before edits — Reports at Column 6, Positions at Column 7. Shift Reports to Column 5, add Backup at Column 6, keep Positions at Column 7. Adjust margins accordingly.
  </action>
  <verify>
    <automated>dotnet build src/FileTracker.sln --no-restore</automated>
  </verify>
  <done>
    Backup button visible in MainWindow Documents tab toolbar. Clicking it opens a folder picker dialog, creates a timestamped .zip backup in the chosen folder, and shows a success MessageBox with the file path. Per D-02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|-----------|-------------|
| User input → backup destination | User chooses any folder; backup writes there |
| Temp directory → backup staging | Staging data in OS temp, cleaned after zip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | BackupService.CreateBackupAsync | mitigate | Zip file integrity verified in tests; user-visible on restore (Plan 02) |
| T-04-02 | Information Disclosure | Temp staging directory | mitigate | Temp dir uses random GUID subfolder, cleaned in finally block |
| T-04-03 | Denial of Service | Large attachments directory | mitigate | Document backup is bounded by local disk; no remote attack vector |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — System.IO.Compression and SqliteConnection.BackupDatabase are built-in .NET APIs |
</threat_model>

<verification>
Build passes with 0 errors. All BackupService tests pass. Manual smoke test: run app, click Backup, choose folder, verify .zip contains .db + attachments/.
</verification>

<success_criteria>
- [x] IBackupService.CreateBackupAsync produces valid .zip with SQLite DB + attachments
- [x] Backup filename matches FileTracker_Backup_YYYY-MM-DD_HHmmss.zip
- [x] Backup button visible in MainWindow, wired to folder picker + success message
- [x] All BackupService integration tests pass
- [x] DATA-01 satisfied: user can backup database and attachments to any folder
</success_criteria>

<output>
Create `.planning/phases/04-data-safety-management/04-01-SUMMARY.md` when done
</output>
